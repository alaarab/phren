<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Phren" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/phren_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:colorAccent">@color/phren_accent</item>
    </style>

    <!-- A translucent card over whatever is on screen, like Siri's snippet. -->
    <style name="Theme.Phren.Capture" parent="Theme.Phren">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
