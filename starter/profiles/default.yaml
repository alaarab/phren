name: default
description: Default profile, all projects
projects:
  # "global" is a special directory for cross-project config, not a project itself.
  # List your actual projects below:
  - my-api
  - my-frontend
  - my-first-project
