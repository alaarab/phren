# linkedin post — phren

---

## the post

started phren back in february because i was burning tokens repeating myself to claude.

same gotchas, same context, same explanation of why our auth is cursed. every new chat, from scratch.

what started as a couple scripts turned into a full memory layer for ai agents. markdown files, in your own git repo. imagine that — actually owning your data in 2026. lol.

one command:

`npx @phren/cli init`

point it at a repo. that's it. claude remembers now. so does copilot, cursor, codex — same memory across all of them.

add multiple team repos. segregate by project, profile, or machine. phren handles skills, trust decay, contradictions, session continuity — all the stuff you'd eventually build yourself if you were dumb enough to try (i was).

ships as a cli and a vscode extension. fragment graph, session timeline, the works.

open source, mit, on npm as `@phren/cli`. vscode extension is `phren` on the marketplace.

try it. break it. tell me.

---

## alt one-liner hook (if you want shorter)

been burning tokens re-explaining my own repo to claude since january. built phren to fix it. memory for ai agents, markdown in a git repo you own, one command to install. cli + vscode extension. mit. link below.

---

## notes

- first line is the hook. "started phren back in february because i was burning tokens repeating myself to claude" is the whole pitch — it states the problem, the solution, and the credibility (you built it, you shipped it, it's been running).
- link in first comment, not the post.
- pin a comment with: cli → `npm i -g @phren/cli`, vscode → marketplace link, repo → github link.
- screenshot the vscode extension sidebar or the fragment graph. visual proof it's a real thing.
