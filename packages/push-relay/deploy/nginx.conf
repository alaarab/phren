# For a host that already runs nginx: add this server block, then
# `certbot --nginx -d push.example.com` for the certificate.
server {
	server_name push.example.com;
	client_max_body_size 16k;
	location / {
		proxy_pass http://127.0.0.1:8787;
		proxy_set_header X-Forwarded-For $remote_addr;
	}
}
